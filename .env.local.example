# Supabase project settings (Settings -> API in the Supabase dashboard)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Shared passcode new members must enter once to join — never exposed to the
# browser (checked server-side only). Change this to whatever you want.
TEAM_INVITE_PASSCODE=
